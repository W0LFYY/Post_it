<h1> Notes </h1>
<% @note.each do |note| %>
  <h2><%= note.title %></h2>
  <%= link_to 'Show Note', note_path(note) %>
  <p><%= link_to 'New Note', new_note_path %></p>
  <%= link_to 'Edit Note', edit_note_path(path)
  <%= link_to 'Destroy Note', note, (method: :delete) %>
<% end %>